listar com veações == pip freeze 

salvando arquivo txt == pip freeze -l > requirements.txt

instalando todos o arquivo da versão txt == pip freeze -r > requirements.txt

para gerar um exe == pyinstaller --onefile (nome do aquivo)